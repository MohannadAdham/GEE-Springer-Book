// Import DEM dataset of France
var franceDEM = ee.ImageCollection("IGN/RGE_ALTI/1M/2_0").first();

// Print the layer to the console
print(franceDEM);

// Center the map
Map.centerObject(franceDEM, 6);

// Add the layer to the map
Map.addLayer(
  franceDEM,
  {
    bands: ['MNT'],
    min: 100,
    max: 3000,
    palette: ['black', '#550000', 'red', 'white']
  },
  'France DEM');
